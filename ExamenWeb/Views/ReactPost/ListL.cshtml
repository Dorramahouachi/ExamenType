@model IEnumerable<Domaine.ReactPost>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeReact)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeReact)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReactPostId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReactPostId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReactPostId })
        </td>
    </tr>
}

</table>
